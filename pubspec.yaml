name: mangalib_app
description: MangaLib + SlashLib client for iOS
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  webview_flutter: ^4.7.0
  shared_preferences: ^2.2.0
  cached_network_image: ^3.3.0

flutter:
  uses-material-design: true
